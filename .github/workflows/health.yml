name: Health Check
on:
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI works" && node -v && java -version 2>&1 | head -1
